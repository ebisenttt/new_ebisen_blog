# Claude Code ガイド

## 依存パッケージアップデート
作業手順は `docs/ai/guides/dependencies-update.md` を参照すること。

## 作業計画書
`docs/ai/plans/tmp.md` が存在する場合はプランの草案。`/plan` コマンドで読み込み・精査を開始する。
計画を修正するときは必ず `tmp.md` も更新すること。
プランが確定したら `docs/ai/plans/README.md` の命名規則に従い `tmp.md` をリネームすること。
